import { Box } from "@mui/material";
import { styled } from "@mui/system";

const FlexD = styled(Box)({
  display: "flex",
  flexWrap: "wrap",
  justifyContent: "space-between",
  alignItems: "center",
  margin: "8px",

});

export default FlexD;